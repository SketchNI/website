export const userType = {
    id: Number,
    name: String,
    email: String,
    email_verified: Boolean,
    created_at: String,
    role: String,
    permissions: Array,
    permissions_count: Number,
    comments_count: Number,
    votes_count: Number,
    roles_count: Number,
    posts_count: Number,
}
